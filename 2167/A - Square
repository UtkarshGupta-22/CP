#include<iostream>using namespace std; int main(){    int n;    cin>>n;    while(n){        int s1,s2,s3,s4;        cin>>s1>>s2>>s3>>s4;        if((s1==s2) && (s2==s3) && (s3==s4) && (s4==s1)) cout<<"YES"<<endl;        else cout<<"NO"<<endl;        n--;    }    return 0;}